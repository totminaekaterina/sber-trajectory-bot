# 🐱 Сбер Квиз - Telegram Mini App

Современное приложение для тестирования студентов по статистике, теории вероятностей и машинному обучению в стиле Сбера.

## 🎯 Возможности

✅ **Frontend**
- Минималистичный дизайн в стиле Сбера с фирменными цветами
- Адаптивный интерфейс с поддержкой темной темы
- Интеграция с Telegram Web App SDK
- Таймер 15 минут на прохождение теста
- Рандомизация вопросов
- Прогресс-бар и мотивационные сообщения

✅ **Backend**
- FastAPI для высокой производительности
- JSON хранилище для вопросов и результатов
- Защита от повторного прохождения теста
- Автоматический подсчет баллов
- Admin панель со статистикой

✅ **Безопасность**
- Уникальная идентификация через Telegram User ID
- Временные метки для отслеживания
- Правильные ответы не отправляются на frontend
- CORS защита

## 📁 Структура проекта

\`\`\`
sber-quiz-miniapp/
├── frontend/
│   ├── index.html          # Главная страница
│   ├── style.css           # Стили в стиле Сбера
│   └── app.js              # Логика приложения
├── backend/
│   ├── main.py             # FastAPI сервер
│   ├── requirements.txt    # Python зависимости
│   └── data/
│       ├── questions.json      # База вопросов
│       └── users_results.json  # Результаты студентов
├── README.md
└── .env.example
\`\`\`

## 🚀 Быстрый старт

### 1. Backend (Python FastAPI)

\`\`\`bash
# Установка зависимостей
pip install -r requirements.txt

# Создайте директорию data и скопируйте туда questions.json
mkdir -p data
cp questions.json data/
echo '{}' > data/users_results.json

# Запуск сервера
python main.py
# или
uvicorn main:app --reload --host 0.0.0.0 --port 8000
\`\`\`

Backend будет доступен на `http://localhost:8000`

### 2. Frontend (HTML/CSS/JS)

\`\`\`bash
# В файле app.js замените API_URL на адрес вашего backend
const API_URL = 'http://localhost:8000';

# Для разработки можно использовать любой HTTP сервер
python -m http.server 3000
# или
npx http-server -p 3000
\`\`\`

Frontend будет доступен на `http://localhost:3000`

### 3. Регистрация в Telegram

1. Создайте бота через [@BotFather](https://t.me/botfather)
2. Получите токен бота
3. Создайте Mini App:
   - /newapp
   - Выберите вашего бота
   - Укажите название, описание
   - Загрузите иконку (можно использовать изображение Сбер Кота)
   - **Web App URL**: адрес вашего frontend (см. Deployment)

## 🌐 Deployment

### Backend (Railway / Render / DigitalOcean)

**Railway:**
\`\`\`bash
# Установите Railway CLI
npm i -g @railway/cli

# Логин и deploy
railway login
railway init
railway up
\`\`\`

**Render:**
1. Подключите GitHub репозиторий
2. Выберите Web Service
3. Build Command: `pip install -r requirements.txt`
4. Start Command: `uvicorn main:app --host 0.0.0.0 --port $PORT`

**Важно**: Backend должен работать по HTTPS для Telegram Mini Apps!

### Frontend (Vercel / Netlify / GitHub Pages)

**Vercel:**
\`\`\`bash
# Установите Vercel CLI
npm i -g vercel

# Deploy
vercel
\`\`\`

**Netlify:**
\`\`\`bash
# Drag & Drop через веб-интерфейс
# или используйте Netlify CLI
netlify deploy --prod
\`\`\`

**GitHub Pages:**
\`\`\`bash
# Просто загрузите frontend файлы в репозиторий
# и включите GitHub Pages в настройках
\`\`\`

**После deploy обновите API_URL в app.js на production адрес backend!**

## 📊 API Endpoints

### Public Endpoints

\`\`\`
GET  /                          # API информация
GET  /questions                 # Получить вопросы (без правильных ответов)
GET  /check-user/{user_id}      # Проверить прохождение теста
POST /submit                    # Отправить ответы
\`\`\`

### Admin Endpoints

\`\`\`
GET /admin/results              # Все результаты студентов
GET /admin/statistics           # Статистика по тестам
\`\`\`

**Пример запроса к админ панели:**
\`\`\`bash
curl http://localhost:8000/admin/results
curl http://localhost:8000/admin/statistics
\`\`\`

## 🎨 Кастомизация

### Изменить вопросы

Отредактируйте `data/questions.json`:

\`\`\`json
{
  "statistics": [
    {
      "id": 1,
      "question": "Ваш вопрос?",
      "options": ["A", "B", "C", "D"],
      "correct": 0
    }
  ],
  "probability": [...],
  "ml": [...]
}
\`\`\`

### Изменить цветовую схему

В `style.css` измените CSS переменные:

\`\`\`css
:root {
    --sber-green: #21A038;
    --sber-green-dark: #1a8030;
    --sber-green-light: #2dc248;
}
\`\`\`

### Изменить время теста

В `app.js`:

\`\`\`javascript
const QUIZ_TIME = 15 * 60; // секунды
\`\`\`

## 🔐 Безопасность

### Production настройки:

1. **Backend:**
   - Настройте CORS только для вашего домена
   - Добавьте аутентификацию для admin endpoints
   - Используйте HTTPS
   - Добавьте rate limiting

2. **Frontend:**
   - Валидация всех входных данных
   - Использование HTTPS
   - Защита от XSS

## 🐛 Отладка

### Тестирование локально без Telegram

В `app.js` закомментируйте проверку:

\`\`\`javascript
// const userId = tg.initDataUnsafe?.user?.id || 'test_user_' + Math.random();
const userId = 'test_user_' + Math.random(); // Для тестирования
\`\`\`

### Просмотр логов Backend

\`\`\`bash
# При запуске uvicorn с --reload автоматически показывает логи
uvicorn main:app --reload --log-level debug
\`\`\`

## 📈 Дальнейшие улучшения

Идеи для расширения:

- [ ] База данных PostgreSQL вместо JSON
- [ ] Аутентификация admin панели (JWT)
- [ ] Экспорт результатов в Excel/CSV
- [ ] Веб-интерфейс для добавления вопросов
- [ ] Различные уровни сложности
- [ ] Таблица лидеров (опционально)
- [ ] Уведомления преподавателю о завершении
- [ ] Аналитика по каждому вопросу (процент правильных ответов)
- [ ] Telegram Bot для управления

## 🤝 Вклад

Если хотите улучшить проект:

1. Fork репозиторий
2. Создайте feature branch
3. Commit изменения
4. Push в branch
5. Откройте Pull Request

## 📄 Лицензия

MIT License

## 👨‍💻 Автор

Разработано для Сбера с ❤️

---

**Вопросы?** Создайте Issue в репозитории!
